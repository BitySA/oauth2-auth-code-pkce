{
  "name": "oauth2-auth-code-pkce",
  "version": "0.0.1",
  "description": "An OAuth 2.0 client that ONLY supports Authorization Code flow with PKCE support.",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc -p ./tsconfig.json && printf '2i\nvar exports = {};\n.\nw\nq\n' | ex index.js",
    "serve": "npm run build && http-server -p 8080"
  },
  "keywords": [
    "oauth",
    "oauth2",
    "pkce"
  ],
  "author": "Lee Fallat",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.6.3"
  }
}
